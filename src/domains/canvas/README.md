# Domínio: Canvas

Estrutura inicial do domínio Canvas.  
Servirá para isolar componentes e lógicas do modo Canvas, seguindo o mesmo padrão modular de Chat.

- `services/canvasBuilder.ts`: encapsula a geração mockada de código/artefatos e é injetado no `useCanvasExperience`.
