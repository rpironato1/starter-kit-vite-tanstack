# Task: Correção Erro node:http Cloudflare Workers

## 1 - Remover dependência @ai-sdk/mcp do package.json
'YOU NEVER MUST EXECUTE THE TASK DIRECTLY, I MUST ONLY ORCHESTRATE IT FOR SUB-AGENTS.'

## 2 - Remover dependência @modelcontextprotocol/sdk do package.json
'YOU NEVER MUST EXECUTE THE TASK DIRECTLY, I MUST ONLY ORCHESTRATE IT FOR SUB-AGENTS.'

## 3 - Excluir pasta node_modules e arquivo package-lock.json
'YOU NEVER MUST EXECUTE THE TASK DIRECTLY, I MUST ONLY ORCHESTRATE IT FOR SUB-AGENTS.'

## 4 - Executar npm install para reinstalar dependências limpas
'YOU NEVER MUST EXECUTE THE TASK DIRECTLY, I MUST ONLY ORCHESTRATE IT FOR SUB-AGENTS.'

## 5 - Validar que src/polyfill.ts não contém imports node:* problemáticos
'YOU NEVER MUST EXECUTE THE TASK DIRECTLY, I MUST ONLY ORCHESTRATE IT FOR SUB-AGENTS.'

## 6 - Verificar wrangler.jsonc mantém nodejs_compat flag
'YOU NEVER MUST EXECUTE THE TASK DIRECTLY, I MUST ONLY ORCHESTRATE IT FOR SUB-AGENTS.'

## 7 - Executar npm run check (zero erros lint)
'YOU NEVER MUST EXECUTE THE TASK DIRECTLY, I MUST ONLY ORCHESTRATE IT FOR SUB-AGENTS.'

## 8 - Executar npm run build (zero erros build)
'YOU NEVER MUST EXECUTE THE TASK DIRECTLY, I MUST ONLY ORCHESTRATE IT FOR SUB-AGENTS.'

## 9 - Executar npx tsc --noEmit (zero erros TypeScript)
'YOU NEVER MUST EXECUTE THE TASK DIRECTLY, I MUST ONLY ORCHESTRATE IT FOR SUB-AGENTS.'

## 10 - Iniciar servidor npm run dev na porta 3027
'YOU NEVER MUST EXECUTE THE TASK DIRECTLY, I MUST ONLY ORCHESTRATE IT FOR SUB-AGENTS.'

## 11 - Testar acesso http://localhost:3027 via Playwright MCP
'YOU NEVER MUST EXECUTE THE TASK DIRECTLY, I MUST ONLY ORCHESTRATE IT FOR SUB-AGENTS.'

## 12 - Validar console devtools sem erros
'YOU NEVER MUST EXECUTE THE TASK DIRECTLY, I MUST ONLY ORCHESTRATE IT FOR SUB-AGENTS.'

## 13 - Gerar relatório final de correção
'YOU NEVER MUST EXECUTE THE TASK DIRECTLY, I MUST ONLY ORCHESTRATE IT FOR SUB-AGENTS.'
