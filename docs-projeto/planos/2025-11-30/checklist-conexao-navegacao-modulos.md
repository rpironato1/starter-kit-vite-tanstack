# Task: Conectar Módulos ao Menu de Navegação

## 1 - Remover Header global duplicado de `__root.tsx`
'YOU NEVER MUST EXECUTE THE TASK DIRECTLY, I MUST ONLY ORCHESTRATE IT FOR SUB-AGENTS.'

## 2 - Implementar navegação real no `Sidebar.tsx` usando `useNavigate` do TanStack Router
'YOU NEVER MUST EXECUTE THE TASK DIRECTLY, I MUST ONLY ORCHESTRATE IT FOR SUB-AGENTS.'

## 3 - Atualizar `index.tsx` para remover prop `onNavigate` obsoleta
'YOU NEVER MUST EXECUTE THE TASK DIRECTLY, I MUST ONLY ORCHESTRATE IT FOR SUB-AGENTS.'

## 4 - Atualizar `photo.tsx` para consistência de navegação
'YOU NEVER MUST EXECUTE THE TASK DIRECTLY, I MUST ONLY ORCHESTRATE IT FOR SUB-AGENTS.'

## 5 - Atualizar `doc.tsx` para consistência de navegação
'YOU NEVER MUST EXECUTE THE TASK DIRECTLY, I MUST ONLY ORCHESTRATE IT FOR SUB-AGENTS.'

## 6 - Atualizar `canvas.tsx` para consistência de navegação
'YOU NEVER MUST EXECUTE THE TASK DIRECTLY, I MUST ONLY ORCHESTRATE IT FOR SUB-AGENTS.'

## 7 - Deletar arquivo `Header.tsx` global obsoleto
'YOU NEVER MUST EXECUTE THE TASK DIRECTLY, I MUST ONLY ORCHESTRATE IT FOR SUB-AGENTS.'

## 8 - Executar validações obrigatórias: `npm run lint`, `npm run build`, `npx tsc --noEmit`
'YOU NEVER MUST EXECUTE THE TASK DIRECTLY, I MUST ONLY ORCHESTRATE IT FOR SUB-AGENTS.'

## 9 - Testes manuais com Playwright MCP - navegação entre módulos
'YOU NEVER MUST EXECUTE THE TASK DIRECTLY, I MUST ONLY ORCHESTRATE IT FOR SUB-AGENTS.'

## 10 - Testes de responsividade mobile/tablet/desktop
'YOU NEVER MUST EXECUTE THE TASK DIRECTLY, I MUST ONLY ORCHESTRATE IT FOR SUB-AGENTS.'

## 11 - Validar zero erros no console DevTools
'YOU NEVER MUST EXECUTE THE TASK DIRECTLY, I MUST ONLY ORCHESTRATE IT FOR SUB-AGENTS.'

## 12 - Gerar relatório final da implementação
'YOU NEVER MUST EXECUTE THE TASK DIRECTLY, I MUST ONLY ORCHESTRATE IT FOR SUB-AGENTS.'
