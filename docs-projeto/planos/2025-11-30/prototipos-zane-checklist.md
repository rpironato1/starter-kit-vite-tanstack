# Task: preparar-prototipos-zane

## 1 - Auditar diretório raiz e confirmar inexistência de `prototipos-zane`
    'YOU NEVER MUST EXECUTE THE TASK DIRECTLY, I MUST ONLY ORCHESTRATE IT FOR SUB-AGENTS.'
## 2 - Criar `prototipos-zane` na raiz mantendo permissões herdadas
    'YOU NEVER MUST EXECUTE THE TASK DIRECTLY, I MUST ONLY ORCHESTRATE IT FOR SUB-AGENTS.'
## 3 - Clonar `rpironato1/zane-ai` em `prototipos-zane/zane-ai`
    'YOU NEVER MUST EXECUTE THE TASK DIRECTLY, I MUST ONLY ORCHESTRATE IT FOR SUB-AGENTS.'
## 4 - Validar `git status` limpo em `prototipos-zane/zane-ai`
    'YOU NEVER MUST EXECUTE THE TASK DIRECTLY, I MUST ONLY ORCHESTRATE IT FOR SUB-AGENTS.'
## 5 - Clonar `rpironato1/zane-ai-ux-interface` em `prototipos-zane/zane-ai-ux-interface`
    'YOU NEVER MUST EXECUTE THE TASK DIRECTLY, I MUST ONLY ORCHESTRATE IT FOR SUB-AGENTS.'
## 6 - Validar `git status` limpo em `prototipos-zane/zane-ai-ux-interface`
    'YOU NEVER MUST EXECUTE THE TASK DIRECTLY, I MUST ONLY ORCHESTRATE IT FOR SUB-AGENTS.'
