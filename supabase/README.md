# Supabase Structure

Estrutura base para atender às regras do AGENTS.md:

- `migrations/`: todos os scripts versionados antes de qualquer alteração no banco.
- `functions/<domínio>/`: cada edge function específica e isolada.

> Nesta etapa ainda não há código backend; os diretórios servem como fundação para futuras integrações.
