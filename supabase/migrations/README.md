# Supabase Migrations

- Crie um arquivo por alteração de schema seguindo o padrão `YYYYMMDDHHMMSS_description.sql`.
- Nenhuma migration deve ser aplicada sem antes existir um arquivo aqui.
- Execute testes com MCP Supabase após cada migração.
